//
//  Data Structures and Algorithms.swift
//  Practice
//
//  Created by J Dayasagar on 24/08/23.
//

import Foundation

/*
func quickSort<T: Comparable>(_array: inout [T], low:Int, high: Int){
    if low < high {
        // Partition the array and get the index of the pivot element
    }
}

func partition<T: Comparable>(_array: inout [T], low:Int, high:Int) -> Int {
    let pivot = array[high]
    // Choose the pivot element, last in this case
    var i = low - 1
}
*/
